import { LoginForm } from "@components/privates/Auth/LoginForm";

export const Login = () => {
  return (
    <div className="relative h-screen overflow-y-auto overscroll-contain bg-purple-03">
      <LoginForm />
    </div>
  );
}